---
baseline_commit: 529195a3e964ce8bad0ac5cfad8879da52ed2edc
---

# Story 1.3: 게시 날짜별 작업 생성과 복원

Status: review

## Story

As a 개인 제작자,
I want 쇼츠의 게시 날짜를 선택하고 날짜별 작업을 보존하고 싶다,
so that 앱을 다시 실행해도 준비하던 작업을 이어갈 수 있다.

## Acceptance Criteria

1. 홈의 게시 날짜 기본값은 현재 날짜이며 `YYYY-MM-DD · Day` 형식으로 표시된다.
2. 달력은 화살표, Home/End, PageUp/PageDown, Enter/Space, Esc의 접근성 grid 패턴을 지원하고 오늘과 선택일을 구분한다.
3. 선택 날짜의 작업이 없으면 내부 작업 ID와 SQLite 레코드, 앱 관리 작업 폴더를 생성한다.
4. renderer 요청의 게시 날짜와 작업 ID를 main과 Python 경계에서 각각 검증하며 renderer는 SQLite나 파일 시스템에 직접 접근하지 않는다.
5. 앱 재실행 또는 날짜 재선택 시 저장 상태와 입력 메타데이터를 복원하고 실제 경로와 SQLite 기록을 대조한다.
6. 빈 DB와 이전 스키마 DB에 순차 마이그레이션을 적용하며 게시 날짜와 내부 작업 ID를 분리해 저장한다.

## Tasks / Subtasks

- [x] Story 1.1·1.2 구현을 확인하고 기존 main/preload/renderer 계약을 읽는다. (AC: 1~6)
- [x] Python storage 계층에 `001_create_jobs.sql`, migration runner, job repository를 구현한다. (AC: 3, 5, 6)
  - [x] `jobs`는 내부 `id`, 시간대 없는 유일 `publish_date`, 상태, 제목, 작업/결과 경로와 생성·수정 시각을 저장한다.
  - [x] `PRAGMA foreign_keys = ON`과 `schema_migrations` 기반 순차·중복 적용 방지를 테스트한다.
- [x] 앱 관리 루트와 `jobs/<publish-date>/{input,output,temp,logs}` 경로를 main/Python 경계에서 계산·검증한다. (AC: 3~5)
- [x] `getOrCreateJobForDate`와 작업 DTO를 공유 계약, preload, IPC, Python command/event에 연결한다. (AC: 3~5)
- [x] Date Picker와 Calendar Popover를 job-editor에 구현하고 선택 작업을 복원한다. (AC: 1, 2, 5)
- [x] repository·migration·IPC 계약·달력 키보드·재실행 복원 테스트를 추가한다. (AC: 1~6)

## Dev Notes

- SQLite의 최초 소유 Story다. 별도 Node SQLite 모듈을 추가하지 않고 Python 표준 `sqlite3`만 사용한다.
- 게시 날짜는 `YYYY-MM-DD`, 시각은 UTC ISO 8601이다. 폴더명과 DB ID를 동일시하지 않는다.
- SQLite가 메타데이터 기준이고 파일 시스템은 입력·결과 존재의 기준이다. 불일치는 숨기지 말고 DTO 상태로 투영한다.
- `app.getPath('userData')` 아래 승인된 앱 관리 루트를 main에서 결정하되 Python에도 정규화된 루트와 ID를 전달하고 재검증한다.
- 달력은 roving tabindex를 사용해 한 날짜 셀만 tab 순서에 둔다. APG 예제를 그대로 복사하지 말고 실제 보조기술 테스트를 수행한다.
- Story 1.4의 `job_inputs` 테이블과 파일 등록, Story 3.3의 완료 날짜 재생성은 선행 구현하지 않는다.

### Expected File Changes

- `engine/migrations/001_create_jobs.sql`
- `engine/gracetree_engine/storage/{migrations.py,job_repository.py}`
- `apps/desktop/src/main/{ipc/register-job-handlers.ts,files/managed-paths.ts}`
- `packages/contracts/src/desktop-api.ts`
- `apps/desktop/src/renderer/src/features/job-editor/`
- 대응 TypeScript/Python/integration 테스트

### Testing Requirements

- 빈 DB, 직전 스키마, 재실행, 중복 publish date, 잘못된 날짜·작업 ID, 관리 루트 이탈을 검증한다.
- 달력의 모든 키와 오늘/선택 상태, Esc 닫기·포커스 복원을 검증한다.
- renderer bundle에 SQLite·`fs` 접근이 없고 기존 health 계약과 앱 셸 테스트가 유지되는지 확인한다.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#story-13-게시-날짜별-작업-생성과-복원]
- [Source: _bmad-output/planning-artifacts/architecture.md#데이터-아키텍처]
- [Source: _bmad-output/planning-artifacts/architecture.md#사용자-데이터-디렉터리]
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-gracetree-shorts-studio-2026-06-19/EXPERIENCE.md#accessibility-floor]
- https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/examples/datepicker-dialog/
- https://docs.python.org/3/library/sqlite3.html
- https://www.sqlite.org/foreignkeys.html

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Implementation Plan

- Python storage 계층에 순차 SQLite migration과 날짜별 job repository를 먼저 구현한다.
- main이 관리 루트와 UUID를 검증하고 장기 실행 JSON Lines 엔진을 통해 job DTO를 조회·생성하도록 연결한다.
- 공유 `DesktopApi` 계약과 preload IPC 표면을 확장하고 renderer에는 날짜 선택과 복원 DTO만 전달한다.
- 접근성 grid 달력과 재실행·경계 검증 테스트를 추가한 뒤 전체 CI와 Electron E2E를 실행한다.

### Debug Log References

- Story 1.1·1.2 완료 커밋과 main/preload/renderer, Python health 프로토콜, Architecture의 SQLite·사용자 데이터 경계를 확인했다.
- migration/repository RED 테스트는 storage 모듈 부재로 실패했고, 구현 후 빈 DB·이전 schema·중복 날짜·잘못된 날짜/UUID·루트 이탈·경로 누락 8개 테스트가 통과했다.
- main 관리 경로 RED 테스트는 모듈 부재로 실패했고, 구현 후 유효 날짜/UUID와 루트 이탈 방지 6개 테스트를 포함한 desktop 23개 테스트가 통과했다.
- 계약·IPC·Python round-trip RED 테스트는 신규 command/event와 handler 부재로 실패했고, 구현 후 contracts 7개, Python 18개, desktop 26개 테스트와 타입 검사가 통과했다.
- Date Picker RED 테스트는 컴포넌트 부재로 실패했고, 구현 후 모든 달력 키·roving tabindex·오늘/선택·Esc/Enter/Space·복원 상태 테스트가 통과했다.
- preload runtime bundle이 workspace 계약을 require해 sandbox에서 실패한 문제를 type-only 계약 import와 로컬 typed channel 상수로 수정했다.
- `PYTHON=.venv/bin/python pnpm run ci`, desktop production build와 Electron 재실행 복원 E2E가 통과했다.

### Completion Notes List

- Ultimate context engine analysis completed - comprehensive developer guide created.
- Story 1.1·1.2 기반과 기존 보안 경계를 확인하고 Story 1.3 구현 기준선을 `529195a`로 기록했다.
- Python 표준 sqlite3 기반 migration runner와 날짜별 job repository를 구현해 게시 날짜와 내부 UUID를 분리 저장하고 관리 폴더 상태를 대조한다.
- main에서 `GraceTreeData/jobs/<publish-date>` 경로를 계산하고 날짜·UUID·관리 루트 경계를 검증한다.
- typed `getOrCreateJobForDate` preload API, IPC 검증 handler와 장기 실행 JSON Lines Python client를 연결하고 DTO/event를 양쪽 schema로 검증한다.
- 기본값이 로컬 오늘인 `YYYY-MM-DD · Day` Date Picker와 6주 접근성 grid 달력을 구현하고 날짜 재선택 시 저장 작업을 복원한다.
- 빈 DB·이전 schema·중복 날짜·경로 불일치·IPC 계약·renderer 권한 경계·앱 재실행 복원을 자동 검증해 Story 1.3을 `review` 상태로 전환했다.

### File List

- `_bmad-output/implementation-artifacts/1-3-게시-날짜별-작업-생성과-복원.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `apps/desktop/src/main/files/managed-paths.test.ts`
- `apps/desktop/src/main/files/managed-paths.ts`
- `apps/desktop/src/main/index.ts`
- `apps/desktop/src/main/ipc/register-job-handlers.test.ts`
- `apps/desktop/src/main/ipc/register-job-handlers.ts`
- `apps/desktop/src/main/jobs/engine-client.ts`
- `apps/desktop/src/preload/desktop-api.test.ts`
- `apps/desktop/src/preload/desktop-api.ts`
- `apps/desktop/src/renderer/src/App.test.tsx`
- `apps/desktop/src/renderer/src/App.tsx`
- `apps/desktop/src/renderer/src/features/job-editor/DatePicker.module.css`
- `apps/desktop/src/renderer/src/features/job-editor/DatePicker.test.tsx`
- `apps/desktop/src/renderer/src/features/job-editor/DatePicker.tsx`
- `apps/desktop/src/renderer/src/renderer-boundary.test.ts`
- `apps/desktop/tests/e2e/desktop-shell.spec.ts`
- `engine/gracetree_engine/cli.py`
- `engine/gracetree_engine/storage/__init__.py`
- `engine/gracetree_engine/storage/job_repository.py`
- `engine/gracetree_engine/storage/migrations.py`
- `engine/migrations/001_create_jobs.sql`
- `engine/tests/test_contracts.py`
- `engine/tests/test_engine_process.py`
- `engine/tests/test_job_repository.py`
- `engine/tests/test_migrations.py`
- `packages/contracts/fixtures/valid-get-or-create-job.json`
- `packages/contracts/fixtures/valid-job-loaded.json`
- `packages/contracts/schemas/engine-command.schema.json`
- `packages/contracts/schemas/engine-event.schema.json`
- `packages/contracts/src/desktop-api.ts`
- `packages/contracts/src/protocol.ts`
- `packages/contracts/tests/protocol.test.ts`
- `tests/integration/engine-health.mjs`

### Change Log

- 2026-06-20: SQLite job 저장소, 관리 경로, JSON Lines/IPC/preload 계약, 접근성 Date Picker와 재실행 복원 검증 구현.
