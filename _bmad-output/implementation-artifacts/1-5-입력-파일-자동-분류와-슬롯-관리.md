---
baseline_commit: e9f63097a5885822a71370bafdaeef340d872455
---

# Story 1.5: 입력 파일 자동 분류와 슬롯 관리

Status: done

## Story

As a 개인 제작자,
I want 등록한 파일이 역할별 슬롯에 자동 배치되고 문제 파일을 수정하고 싶다,
so that 각 파일의 용도를 직접 반복 지정하지 않고 입력을 준비할 수 있다.

## Acceptance Criteria

1. 이미지와 텍스트를 썸네일·스크립트 후보로 분류하고 역할·파일명을 File Slot에 표시한다.
2. `voice.*`, `bgm.*` 파일명 규칙으로 오디오 역할을 배치하며 확장자만으로 불확실한 역할을 결정하지 않는다.
3. 역할이 불명확하면 미분류로 유지하고 역할 선택 또는 제거를 제공한다.
4. 동일 슬롯 후보가 여러 개면 충돌로 표시하고 임의 활성 입력을 선택하지 않는다.
5. 제거 시 SQLite 메타데이터와 관리 사본을 일관되게 제거하고 관리 루트를 검증한다.
6. 교체는 새 파일 복사·검증 성공 후 커밋하며 실패 시 기존 정상 입력을 유지한다.
7. 정상·충돌·누락·미분류 상태를 색상 외 아이콘·문구로 제공하고 모든 동작을 키보드로 실행할 수 있다.

## Tasks / Subtasks

- [x] 순수·결정적 input classifier와 명시적 role enum을 공유 계약에 정의한다. (AC: 1~4)
- [x] repository에서 후보·활성·충돌·미분류 상태를 보존하고 DTO로 투영한다. (AC: 3~5)
- [x] 제거는 관리 루트 검증 후 DB와 파일을 일관되게 갱신하고 실패를 숨기지 않는다. (AC: 5)
- [x] 교체는 새 사본을 별도 임시 경로에서 검증한 뒤 원자적으로 메타데이터를 전환한다. (AC: 6)
- [x] File Slot UI, 역할 선택, 제거·교체 확인과 키보드 동작을 구현한다. (AC: 1~7)
- [x] 분류 표, 충돌, 제거·교체 실패, 접근성 회귀 테스트를 추가한다. (AC: 1~7)

## Dev Notes

- 파일 확장자와 MIME은 후보 판단 자료이지 신뢰 경계가 아니다. 실제 파일 가독성 검증과 파일명 규칙을 함께 적용한다.
- 자동 분류가 확실하지 않으면 미분류가 정답이다. “첫 파일 우선” fallback을 만들지 않는다.
- 제거·교체는 데이터 삭제를 포함하므로 Story AC가 허용한 앱 관리 사본에만 제한한다. 출처 파일과 기존 정상 입력은 보호한다.
- 상태는 `ready`, `missing`, `conflict`, `unclassified`, `invalid`처럼 계약으로 고정하고 UI 문자열과 분리한다.

### Expected File Changes

- `engine/gracetree_engine/inputs/{classifier.py,input_service.py}`
- `engine/gracetree_engine/storage/input_repository.py`
- `packages/contracts/src/desktop-api.ts`
- `apps/desktop/src/main/ipc/register-file-handlers.ts`
- `apps/desktop/src/renderer/src/features/job-editor/`

### Testing Requirements

- 대소문자·복수 점 파일명·확장자 위장·동일 역할 복수 후보·수동 역할 지정의 결정성을 검증한다.
- 교체 실패 단계별로 기존 파일·DB가 보존되는지, 제거 대상이 루트 밖이면 차단되는지 검증한다.
- File Slot의 accessible name, 상태 설명, Enter/Space와 포커스 복원을 확인한다.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#story-15-입력-파일-자동-분류와-슬롯-관리]
- [Source: _bmad-output/planning-artifacts/architecture.md#입력-등록-및-설정--fr-1fr-10]
- [Source: _bmad-output/planning-artifacts/ux-designs/ux-gracetree-shorts-studio-2026-06-19/DESIGN.md#components]

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Implementation Plan

- 파일명과 확장자만 입력으로 받는 순수 classifier를 만들고 확실하지 않은 오디오는 `unclassified`로 유지한다.
- `job_inputs` 역할·상태 계약을 순차 마이그레이션으로 확장하고 역할별 후보 수에 따라 전체 상태를 재계산한다.
- 제거·교체는 관리 입력 경로를 재검증하고 임시 사본·백업·SQLite 트랜잭션으로 실패 시 기존 입력을 복원한다.
- main/preload 공유 계약을 확장하고 renderer에는 제한된 역할 지정·제거·교체 API와 최신 입력 DTO만 제공한다.
- File Slot에 정상·충돌·누락·미분류 상태, 역할 선택, 명시적 확인, 키보드 포커스 복원을 구현한다.

### Completion Notes List

- Ultimate context engine analysis completed - comprehensive developer guide created.
- 대소문자·복수 점 파일명을 결정적으로 분류하며 이미지·텍스트와 `voice.*`·`bgm.*`만 자동 역할 지정하고 그 외 후보는 미분류로 유지한다.
- 동일 역할 후보 전체를 `conflict`로 저장하고 수동 역할 변경·제거 후 상태를 재계산하는 DTO 계약과 003 마이그레이션을 추가했다.
- 관리 루트 밖 제거를 차단하고 파일 삭제 실패 시 DB를 유지하며, 교체 복사 실패 시 기존 파일·메타데이터를 보존하도록 구현했다.
- File Slot UI에 역할·파일명과 정상·충돌·누락·미분류 아이콘·문구, 역할 선택, 제거·교체 확인과 포커스 복원을 추가했다.
- desktop 65개, contracts 11개, Python 63개 테스트와 통합 스모크, Python lock, production build, Electron E2E를 통과했다.

### File List

- `_bmad-output/implementation-artifacts/1-5-입력-파일-자동-분류와-슬롯-관리.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `apps/desktop/src/main/ipc/register-file-handlers.test.ts`
- `apps/desktop/src/main/ipc/register-file-handlers.ts`
- `apps/desktop/src/main/jobs/engine-client.test.ts`
- `apps/desktop/src/main/jobs/engine-client.ts`
- `apps/desktop/src/preload/desktop-api.test.ts`
- `apps/desktop/src/preload/desktop-api.ts`
- `apps/desktop/src/renderer/src/features/job-editor/FileSlot.tsx`
- `apps/desktop/src/renderer/src/features/job-editor/InputDropZone.module.css`
- `apps/desktop/src/renderer/src/features/job-editor/InputDropZone.test.tsx`
- `apps/desktop/src/renderer/src/features/job-editor/InputDropZone.tsx`
- `apps/desktop/src/renderer/src/features/job-editor/JobEditor.tsx`
- `apps/desktop/tests/e2e/desktop-shell.spec.ts`
- `engine/gracetree_engine/cli.py`
- `engine/gracetree_engine/inputs/__init__.py`
- `engine/gracetree_engine/inputs/classifier.py`
- `engine/gracetree_engine/inputs/input_service.py`
- `engine/gracetree_engine/storage/input_repository.py`
- `engine/migrations/003_classify_job_inputs.sql`
- `engine/tests/test_engine_process.py`
- `engine/tests/test_input_classifier.py`
- `engine/tests/test_input_repository.py`
- `engine/tests/test_migrations.py`
- `packages/contracts/fixtures/valid-input-files-registered.json`
- `packages/contracts/schemas/engine-command.schema.json`
- `packages/contracts/schemas/engine-event.schema.json`
- `packages/contracts/src/desktop-api.ts`
- `packages/contracts/src/protocol.ts`
- `packages/contracts/tests/protocol.test.ts`

### Review Findings

- [x] [Review][Dismiss] `replace_input` 파일명 변경 시 역할 재분류 — UX상 도달 불가 시나리오, 의도된 동작
- [x] [Review][Dismiss] `remove_input` UnboundLocalError — Python try/finally 의미상 불가, false positive
- [x] [Review][Dismiss] 전체 거부 배치 `inputs: null` — 의도된 "변경 없음" 신호
- [x] [Review][Dismiss] `inserted = None` AttributeError — 동일 트랜잭션 내 불가
- [x] [Review][Dismiss] CLI 미인식 커맨드 라우팅 — 스키마 검증이 먼저 가드
- [x] [Review][Dismiss] `register_batch` KeyError — 커밋 후 즉시 조회 가능
- [x] [Review][Dismiss] `runAction` silent no-op — React state batching으로 불가
- [x] [Review][Dismiss] `InputService.manage` 암묵적 else — 스키마 검증이 먼저 가드
- [x] [Review][Dismiss] 대소문자 무관 파일시스템 경로 검증 — 실제 도달 불가
- [x] [Review][Patch] `toSelectedFile` 경로 해석 실패 시 빈 문자열 반환 [apps/desktop/src/preload/desktop-api.ts] — ✅ 수정 완료: 즉시 '파일 경로를 읽을 수 없습니다.' 예외로 교체
- [x] [Review][Defer] `os.link` exFAT·외장드라이브 크래시 [engine/gracetree_engine/storage/input_repository.py] — deferred: 외장드라이브 미지원 결정
- [x] [Review][Defer] `assign_role` autocommit 추측 — diff만으로 검증 불가 — deferred, pre-existing
- [x] [Review][Defer] FileSlot `replacement` stale 상태 — 현재 버그 없으나 향후 위험 — deferred, pre-existing
- [x] [Review][Defer] 스키마 `conflict` 이름 중의성 — deferred, pre-existing
- [x] [Review][Defer] `invalid` 상태 E2E 테스트 없음 — deferred, pre-existing

### Change Log

- 2026-06-21: Story 1.5 자동 분류, 슬롯 상태, 안전한 제거·교체, File Slot UI와 전체 회귀 테스트를 구현하고 상태를 `review`로 전환했다.
