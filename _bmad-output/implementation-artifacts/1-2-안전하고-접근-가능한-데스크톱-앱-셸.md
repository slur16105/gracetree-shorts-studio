---
baseline_commit: 38ceeee9fa28a5cefb45cfe2bf58bc96bd050b96
---

# Story 1.2: 안전하고 접근 가능한 데스크톱 앱 셸

Status: review

## Story

As a 개인 제작자,
I want Windows와 macOS에서 실행되는 안전한 데스크톱 앱 셸을 사용하고 싶다,
so that 영상 제작 기능을 일관된 로컬 환경에서 사용할 수 있다.

## Acceptance Criteria

1. **Given** 사용자가 앱을 실행했을 때
   **When** 메인 창이 표시되면
   **Then** 홈, 사용 가이드, 설정 진입점이 있는 기본 앱 셸이 표시된다.
   **And** 1180×720의 권장 레이아웃과 다크 디자인 토큰이 적용된다.

2. **Given** renderer가 운영체제 기능을 사용해야 할 때
   **When** 파일이나 프로세스 기능에 접근하려 하면
   **Then** renderer는 Node.js API에 직접 접근할 수 없다.
   **And** `contextIsolation`, sandbox 및 로컬 리소스 전용 CSP가 활성화된다.

3. **Given** renderer와 main 프로세스가 통신할 때
   **When** preload API를 사용하면
   **Then** 타입이 지정된 허용 API만 노출된다.
   **And** 임의 파일 시스템 및 프로세스 실행 기능은 노출되지 않는다.

4. **Given** 사용자가 키보드만 사용할 때
   **When** `Tab`, `Enter`, `Space`로 기본 앱 셸을 탐색하면
   **Then** 모든 전역 진입점에 접근할 수 있다.
   **And** 현재 포커스와 활성 화면이 색상 외의 방식으로도 구분된다.

5. **Given** 앱 셸의 상태와 선택 지점을 표시할 때
   **When** 디자인 토큰을 적용하면
   **Then** 브랜드 분홍색은 선택, 녹색은 준비·진행·성공, 경고와 위험 색상은 확인 필요·실패 의미로 사용한다.
   **And** 일반 텍스트 4.5:1, 큰 텍스트와 비텍스트 UI 3:1 이상의 목표 대비를 유지한다.

6. **Given** 운영체제에서 모션 감소가 활성화되어 있을 때
   **When** 앱 셸과 상태 피드백을 표시하면
   **Then** 반복 펄스와 불필요한 이동 애니메이션을 줄인다.
   **And** 상태 의미를 애니메이션에만 의존하지 않는다.

7. **Given** 창이 1180×720의 권장 크기보다 작아질 때
   **When** 핵심 레이아웃을 표시하면
   **Then** 모바일 스택으로 재배치하지 않는다.
   **And** 주요 버튼과 진행률이 잘리지 않도록 최소 창 크기를 제한하거나 안내한다.

8. **Given** 네트워크 연결이 없을 때
   **When** 앱을 실행하고 기본 화면을 탐색하면
   **Then** 앱 셸과 로컬 기능은 정상 동작한다.
   **And** 외부 연결을 요구하는 오류나 배너를 표시하지 않는다.

9. **Given** Windows 또는 macOS의 디스플레이 배율이 200%일 때
   **When** 홈, 가이드 및 설정 화면을 탐색하면
   **Then** 핵심 정보와 주요 동작을 확대·스크롤을 통해 사용할 수 있다.
   **And** 버튼, 포커스 표시 및 상태 문구가 겹치거나 잘리지 않는다.

## Tasks / Subtasks

- [x] 1. Story 1.1 기반을 확인하고 안전한 BrowserWindow 구성을 적용한다. (AC: 1, 2, 7, 8)
  - [x] Story 1.1 산출물과 검증이 완료되지 않았다면 먼저 Story 1.1을 구현한다. 구조를 임의로 대신 만들지 않는다.
  - [x] 기존 `apps/desktop/src/main/index.ts`를 완전히 읽고 스타터의 수명주기·개발 URL·production 파일 로딩 동작을 보존한다.
  - [x] 메인 창에 `width: 1180`, `height: 720`, `minWidth: 1180`, `minHeight: 720`을 적용하고 플랫폼별 기본 창 동작을 유지한다.
  - [x] `nodeIntegration: false`, `contextIsolation: true`, `sandbox: true`, `webviewTag: false`, 검증된 preload 경로를 명시한다.
  - [x] `will-navigate`에서 현재 문서 이외의 navigation을 차단하고 `setWindowOpenHandler`에서 모든 새 창 생성을 거부한다.
  - [x] 앱 시작이 네트워크 요청이나 외부 CDN 자원에 의존하지 않는지 확인한다.

- [x] 2. 최소 권한 preload 경계와 renderer 타입을 정리한다. (AC: 2, 3)
  - [x] 기존 `apps/desktop/src/preload/index.ts`와 Story 1.1의 `packages/contracts`를 읽고 계약 소유권을 유지한다.
  - [x] `packages/contracts/src/desktop-api.ts`에 메서드가 없는 `DesktopApi` 공개 타입과 `DESKTOP_API_KEY = "desktopApi"` 상수를 정의하고 package export를 추가한다.
  - [x] `apps/desktop`이 `@gracetree/contracts`를 `workspace:*`로 참조하게 하고 preload와 renderer 전역 선언이 같은 공개 타입을 import하도록 한다.
  - [x] `contextBridge`를 통해 `desktopApi` 단일 namespace에 동결된 빈 객체만 노출한다. 이는 경계 존재를 검증하기 위한 빈 표면이며 성공하는 stub이나 권한 API가 아니다.
  - [x] Electron의 `ipcRenderer`, Node 객체, callback/event 객체 전체를 renderer에 전달하지 않는다.
  - [x] 파일·프로세스 API와 이후 Story가 소유할 메서드의 빈 구현 또는 성공하는 stub을 추가하지 않는다.
  - [x] preload 표면 테스트에서 `fs`, `path`, `child_process`, 원시 `ipcRenderer`, 임의 channel 호출이 노출되지 않는지 검증한다.

- [x] 3. Studio Black 디자인 토큰과 전역 스타일 기반을 구현한다. (AC: 1, 5, 6, 7, 9)
  - [x] `styles/tokens.css`에 승인된 색상, 타이포그래피, 4px 간격, 반경, 78px rail, 70px bottom status 토큰을 CSS custom properties로 정의한다.
  - [x] `styles/globals.css`에 box sizing, body/root 높이, 기본 다크 표면, 폰트 fallback, 키보드 포커스, 선택·비활성 상태, 200% 확대 시 스크롤 가능한 overflow 규칙을 정의한다.
  - [x] 외부 웹폰트나 CDN을 로드하지 않고 `Inter, Pretendard, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif` fallback을 사용한다.
  - [x] `prefers-reduced-motion: reduce`에서 비필수 transition/animation을 제거하거나 최소화한다.
  - [x] 토큰의 주요 텍스트 조합은 4.5:1, 큰 텍스트·비텍스트 UI와 focus indicator는 3:1 목표를 자동 또는 계산 기반 테스트로 확인한다.

- [x] 4. 전역 앱 셸과 탐색 진입점을 구현한다. (AC: 1, 4, 7, 8, 9)
  - [x] `App.tsx`는 78px 사이드바, `<main>` 콘텐츠 영역, 70px 하단 상태 영역의 전역 구조를 소유한다.
  - [x] 홈과 사용 가이드는 React Router 없이 앱 수준의 명시적 view state로 전환한다.
  - [x] 설정 진입점은 현재 view를 바꾸지 않고 모달 shell을 연다. 실제 리소스 목록·교체 기능은 Story 1.7까지 구현하지 않는다.
  - [x] 홈에는 후속 기능을 위한 구조적 영역과 현재 사용 가능한 기능이 없다는 빈 상태만 제공한다. 가짜 완료 데이터, 가짜 진행률, 작동하는 것처럼 보이는 생성 버튼은 넣지 않는다.
  - [x] 가이드 view는 제목과 “Story 1.8에서 콘텐츠 제공”에 해당하는 사용자용 최소 빈 상태만 제공하고 세부 가이드 콘텐츠를 선행 구현하지 않는다.
  - [x] 홈·가이드·설정 아이콘 버튼은 실제 `<button>`을 사용하고 각각 `홈`, `사용 가이드`, `공통 리소스 설정` 접근성 이름을 제공한다.
  - [x] 활성 view는 `aria-current` 또는 동등한 상태와 시각 표면을 함께 사용한다. 포커스, 활성, hover를 같은 상태로 취급하지 않는다.

- [x] 5. 설정 모달 shell과 키보드·포커스 동작을 구현한다. (AC: 1, 4, 9)
  - [x] 설정 버튼으로 모달을 열면 제목과 닫기 동작이 있는 단일 dialog shell을 표시한다.
  - [x] 모달은 `role="dialog"`, `aria-modal="true"`, 연결된 accessible name을 제공한다.
  - [x] 열릴 때 모달 내부로 포커스를 이동하고 `Tab`/`Shift+Tab`을 내부에 제한하며 `Esc`와 닫기 버튼으로 닫는다.
  - [x] 닫은 뒤 포커스를 설정 호출 버튼으로 복원하고 모달 중첩을 허용하지 않는다.
  - [x] 리소스 파일명·상태·교체 동작과 파일 선택 IPC는 Story 1.7 범위로 남긴다.

- [x] 6. CSP와 로컬 자원 정책을 적용한다. (AC: 2, 8)
  - [x] `file://` production 로딩에 적용되는 renderer HTML meta CSP를 사용하고 `default-src 'self'`를 기준으로 필요한 local script/style/image/font만 허용한다.
  - [x] `http:`, `https:`, 원격 script/font/image, `unsafe-eval`을 production 정책에서 허용하지 않는다.
  - [x] 개발 HMR에 필요한 완화가 있다면 development에만 제한하고 production CSP 테스트가 이를 허용하지 않는지 확인한다.
  - [x] 모든 shell style을 정적 CSS 파일로 이동하고 production CSP에서 `unsafe-inline`, `unsafe-eval`, 광범위한 `*` source를 사용하지 않는다.

- [x] 7. 컴포넌트·보안·접근성 검증을 추가한다. (AC: 1~9)
  - [x] React 컴포넌트 테스트를 위해 `@testing-library/react`, `@testing-library/user-event`, `@testing-library/jest-dom`, `jsdom`을 dev dependency로 추가하고 정확한 해석 버전을 `pnpm-lock.yaml`로 고정한다.
  - [x] Electron 창·오프라인·200% 확대 검증은 Architecture에서 승인된 Playwright 1.61 계열의 Electron 지원을 사용하고 정확한 버전을 lockfile로 고정한다.
  - [x] React Testing Library로 전역 진입점의 accessible name, view 전환, 활성 상태, 설정 모달 열기·닫기·포커스 복원을 검증한다.
  - [x] 키보드 `Tab`, `Enter`, `Space`, `Esc` 경로를 사용자 관점 테스트로 검증한다.
  - [x] main의 창 옵션과 navigation policy를 순수 함수 또는 별도 모듈로 분리해 Electron 런타임을 실행하지 않는 단위 테스트에서도 보안 옵션을 검증할 수 있게 한다.
  - [x] preload 테스트에서 공개 key가 `desktopApi` 하나뿐이고 값이 동결된 빈 객체이며 raw IPC·Node API가 없는지 검증한다.
  - [x] Playwright Electron 테스트에서 1180×720 최소 창, 200% 확대, 홈·가이드·설정 핵심 컨트롤의 가시성과 overflow 접근성을 확인한다.
  - [x] Playwright Electron 테스트에서 HTTP(S) 요청을 기록·차단한 상태로 shell을 로드하고 외부 요청이 0건인지 검증한다.
  - [x] 타입 검사, Vitest, 기존 Story 1.1 계약 테스트와 전체 CI를 실행해 기반 회귀가 없는지 확인한다.

## Dev Notes

### Developer Context

- 이 Story는 “페이지별 전체 UI”를 완성하는 작업이 아니라 모든 후속 화면이 공유할 데스크톱 shell, 보안 경계, 디자인 토큰과 접근성 기반을 만든다.
- Story 1.1은 커밋 `38ceeee`에서 `done`으로 완료됐고 현재 작업 트리는 clean이다. main/preload/renderer, contracts, Python 엔진, lockfile과 CI가 실제로 존재하므로 이 Story는 아래 현재 구현을 보존하며 국소 수정한다.
- Story 1.7이 공통 리소스 설정 내용과 교체 기능을, Story 1.8이 사용 가이드의 실제 콘텐츠를 소유한다. 이 Story에서는 진입점과 shell/empty state만 만든다.
- Story 1.3 이후의 날짜 선택, 파일 등록, 완료 목록, 작업 상태를 가짜 데이터로 채우지 않는다. 후속 Story가 안정적으로 삽입될 수 있는 semantic region과 레이아웃 경계만 제공한다.
- UX 참고 시안의 Direction A “Studio Black”을 사용하되, 시안의 샘플 작업·완료 목록·달력·진행률은 이 Story의 구현 데이터가 아니다.

### Previous Story Intelligence

- Story 1.1은 main/preload/renderer 구조, pnpm workspace, contracts 패키지, 테스트와 CI를 구현하고 병렬 코드 리뷰까지 완료했다.
- Story 1.1에서 금지한 renderer의 Node/파일/프로세스 직접 접근을 그대로 유지한다.
- 공유 공개 타입은 `packages/contracts`에 두고 앱 내부에서 별도 계약을 재정의하지 않는다.
- Story 1.1의 JSON Lines health contract와 Python engine은 이 Story에서 UI 기능으로 노출하거나 확장하지 않는다.
- Story 1.1 테스트와 frozen lockfile CI는 회귀 검증 대상이다. UI 테스트 추가를 이유로 기존 계약 테스트를 비활성화하지 않는다.
- `apps/desktop/src/main/index.ts`는 개발 URL과 production HTML 로딩, macOS activate, window-all-closed 및 electron-toolkit shortcut 동작을 소유한다. 이 수명주기는 보존한다.
- `apps/desktop/src/preload/index.ts`와 `index.d.ts`는 현재 `export {}`만 포함한다. 이 Story에서 최초 `desktopApi` 경계를 추가하되 권한 메서드는 추가하지 않는다.
- renderer는 아직 starter의 `assets/base.css`, `assets/main.css`, Electron 로고와 안내 문구를 사용한다. shell 구현 시 starter 전용 스타일·자산 의존을 제거하고 새 토큰·전역 스타일로 교체한다.
- Story 1.1의 최종 회귀 기준은 Vitest 6개, pytest 9개와 Node↔Python 통합 테스트다.

### Current Implementation Snapshot

- `apps/desktop/src/main/index.ts`
  - 현재: `900×670`, 외부 새 창 차단, sandbox/context isolation 활성, navigation 차단 없음.
  - 변경: 1180×720 최소 크기, 명시적 `webviewTag: false`, navigation policy와 테스트 가능한 창 옵션 분리.
  - 보존: dev URL/production file 로딩, ready-to-show, macOS activate, 종료 수명주기.
- `apps/desktop/src/preload/index.ts`
  - 현재: 공개 bridge 없음.
  - 변경: contracts가 소유하는 타입을 사용하는 동결된 빈 `desktopApi` namespace 공개.
  - 보존: raw IPC·Node API 미노출.
- `apps/desktop/src/renderer/index.html`
  - 현재: local-only CSP지만 `style-src 'unsafe-inline'` 포함.
  - 변경: 정적 CSS 전제로 `unsafe-inline` 제거, 원격 source와 `unsafe-eval` 금지.
- `apps/desktop/src/renderer/src/App.tsx`
  - 현재: Electron starter 로고와 정적 안내만 표시.
  - 변경: semantic shell, view state, sidebar, 빈 홈/가이드, 설정 dialog shell.
- `apps/desktop/src/renderer/src/main.tsx`
  - 현재: `assets/main.css`를 import.
  - 변경: `styles/tokens.css`, `styles/globals.css`를 import하고 테스트 setup과 충돌하지 않게 유지.

### Technical Requirements

- BrowserWindow security:
  - `nodeIntegration: false`
  - `contextIsolation: true`
  - `sandbox: true`
  - `webSecurity`를 비활성화하지 않음
  - remote navigation/new window를 기본 거부
  - `<webview>`를 사용하지 않음
- renderer는 로컬 번들 자원만 사용한다. 외부 CDN, 원격 폰트, analytics, telemetry, network status banner를 추가하지 않는다.
- preload는 메서드 단위의 좁은 bridge만 허용한다. 원시 IPC 송신기, channel 문자열 입력, Node/Electron 객체를 노출하지 않는다.
- 이 Story의 `DesktopApi`는 의도적으로 메서드가 없는 빈 계약이다. Story 1.3 이후 실제 권한 기능이 필요할 때 계약과 구현·테스트를 함께 확장한다.
- 최소 창 크기는 1180×720을 보존한다. 더 작은 viewport에서 모바일 세로 stack으로 바꾸지 않는다.
- 200% 확대에서는 콘텐츠 스크롤을 허용하되 전역 진입점, 상태 영역, dialog의 핵심 동작을 접근할 수 있어야 한다.
- `Tab`은 논리적 DOM 순서를 따르고 `Enter`/`Space`는 native button 동작을 사용한다. 클릭 가능한 `div`에 키보드 동작을 수동 복제하지 않는다.
- 상태 의미는 색상이나 모션 하나에만 의존하지 않는다.

### Architecture Compliance

- renderer view 전환은 React Router가 아니라 작은 앱 수준 view state로 구현한다.
- UI 상태 라이브러리 Zustand 5.0.14 계열은 Architecture에 승인되어 있지만, 이 Story의 shell 상태가 React local state로 충분하면 store를 선행 도입하지 않는다. 실제 공유 상태가 필요한 Story에서 승인 버전을 사용한다.
- 전역 토큰은 CSS custom properties, 기능/컴포넌트 스타일은 CSS Modules를 사용한다.
- renderer는 main 또는 Python 모듈을 import하지 않는다.
- `desktopApi` 공개 계약은 `packages/contracts/src/desktop-api.ts`가 소유한다.
- 홈, 가이드, resources는 향후 `renderer/src/features/` 경계로 확장하되 shell 전용 primitive만 `shared/components`에 둔다.

### UX / Visual Requirements

- 기준 디자인: GraceTree Studio Black.
- 전역 색상:
  - base `#080909`, nav `#090A0A`, panel `#0D0F0F`, raised `#171919`
  - primary text `#F4F5F5`, secondary text `#9FA4A4`
  - selection/brand `#FF2F62`, success `#36D399`, warning `#F2B84B`, danger `#F05D5E`, focus `#7DD3FC`
- 레이아웃:
  - sidebar 78px
  - bottom status 70px
  - desktop horizontal composition
  - 권장 최소 콘텐츠 1180×720
- sidebar button:
  - 44×44px
  - 홈, 사용 가이드, 설정 순서
  - 설정은 sidebar 하단
  - 활성 상태와 keyboard focus를 별도 표현
- 브랜드 분홍색은 선택 지점, 녹색은 준비·진행·성공에만 사용한다. 현재 shell의 빈 상태를 성공색으로 장식하지 않는다.
- icon glyph 문자만으로 의미를 전달하지 말고 accessible name과 상태를 제공한다.

### Expected File Changes

현재 구현을 기준으로 한 UPDATE/NEW 범위:

```text
apps/desktop/src/main/index.ts                         # UPDATE
apps/desktop/src/preload/index.ts                      # UPDATE
apps/desktop/src/renderer/index.html                   # UPDATE: CSP
apps/desktop/src/renderer/src/App.tsx                  # UPDATE
apps/desktop/src/renderer/src/main.tsx                 # UPDATE
apps/desktop/src/renderer/src/styles/tokens.css        # NEW
apps/desktop/src/renderer/src/styles/globals.css       # NEW
apps/desktop/src/renderer/src/components/              # NEW: shell/dialog primitives only
apps/desktop/src/renderer/src/**/*.test.tsx            # NEW
apps/desktop/src/main/**/*.test.ts                     # NEW
apps/desktop/src/preload/**/*.test.ts                  # NEW
apps/desktop/playwright.config.ts                       # NEW if Electron E2E config is separated
tests/e2e/desktop-shell.spec.ts                         # NEW
packages/contracts/src/desktop-api.ts                  # NEW
packages/contracts/package.json                        # UPDATE: export
apps/desktop/package.json                              # UPDATE: workspace contract + test deps/scripts
pnpm-lock.yaml                                        # UPDATE
```

- UPDATE 파일마다 현재 동작, 변경 지점, 보존할 동작을 Dev Agent Record에 기록한다.
- 기존 health contract, Python engine, CI 파일은 기능 변경 대상이 아니며 테스트 회귀만 확인한다.
- starter 전용 `assets/base.css`, `assets/main.css`, Electron 로고·배경 SVG는 더 이상 import되지 않을 때만 삭제한다. 사용 중인 자산을 먼저 삭제하거나 새 shell에서 계속 참조하지 않는다.

### Testing Requirements

- 컴포넌트:
  - 홈·가이드·설정 버튼의 role/name과 tab order
  - 홈↔가이드 전환과 `aria-current`
  - 설정 dialog의 accessible name, focus entry, focus trap, Esc close, trigger focus restoration
  - 버튼의 native Enter/Space activation
- 스타일/접근성:
  - 핵심 token contrast 계산
  - focus indicator가 모든 전역 버튼에서 보임
  - reduced motion media query 존재 및 비필수 motion 제거
  - 200% 확대 시 horizontal/vertical scroll로 핵심 동작 접근 가능
- Electron 보안:
  - BrowserWindow 옵션 회귀 테스트
  - production CSP에 remote source, wildcard, `unsafe-eval`이 없음
  - renderer/preload 공개 표면에 raw IPC·Node API가 없음
  - unexpected navigation/new-window가 차단됨
  - `webviewTag`가 명시적으로 비활성화됨
- 오프라인:
  - shell load 과정에서 외부 HTTP(S) 요청이 없음
  - 폰트·아이콘·CSS가 local bundle 또는 CSS-native 자원으로 동작
- 회귀:
  - Story 1.1 TypeScript/Python 계약 테스트 및 CI 전체 통과
  - `pnpm install --frozen-lockfile`, `pnpm run ci`, desktop production build와 Electron shell E2E 통과

### Scope Boundaries

- 포함: BrowserWindow 보안, CSP, 전역 shell, sidebar 진입점, 홈/가이드 view shell, 설정 dialog shell, 디자인 토큰, global styles, keyboard/focus/reduced-motion/contrast 기반.
- 제외: 날짜 선택기, 파일 drop/slot, 준비 상태, 완료 목록 데이터, 실제 bottom progress, 공통 리소스 조회·교체, 가이드 세부 콘텐츠, SQLite, Python job 기능, FFmpeg, 패키징.
- 제외 기능을 가짜 데이터나 no-op 성공 처리로 보여주지 않는다. 빈 상태는 현재 사용할 수 있는 동작만 설명한다.

### Git Intelligence

- 검증 baseline은 clean HEAD `38ceeee9fa28a5cefb45cfe2bf58bc96bd050b96` (`feat: complete story 1.1 project foundation and CI`)다.
- Story 1.1 커밋은 55개 파일에 Electron 기반, 공유 계약, Python 엔진, CI와 테스트를 추가했다.
- 구현 시작 직전 HEAD와 `git status --short`를 다시 기록하고 이 Story가 만든 변경만 File List에 포함한다.
- Story 1.1의 committed 파일을 재생성하거나 foundation 설정을 되돌리지 않는다.

### Latest Technical Notes

- Electron 공식 보안 지침은 context isolation, renderer sandbox, restrictive CSP, navigation/new-window 제한, IPC sender 검증과 Electron API 비노출을 권고한다.
- Electron의 기본값에 의존하지 말고 Story의 보안 옵션을 코드와 테스트에서 명시한다. Electron 버전 변경 시 기본값이 아닌 프로젝트 계약을 기준으로 검증한다.
- `contextBridge` API는 값·함수 단위의 제한된 표면을 노출하는 데 사용하며 원시 `ipcRenderer`를 넘기지 않는다.
- 현재 lockfile은 Electron `39.8.10`, React `19.2.1`, TypeScript `5.9.3`, Vitest `4.0.16`을 해석한다. 이 Story는 기능 구현을 위해 임의 업그레이드하지 않는다.
- Playwright Electron API는 실험적 지원이므로 테스트를 shell 시작·창 속성·renderer 상호작용·요청 관찰로 제한하고 제품 런타임 코드에 결합하지 않는다.
- WCAG 2.2 기준상 일반 텍스트 4.5:1, 큰 텍스트 3:1 대비 목표를 적용한다.
- `prefers-reduced-motion`은 사용자가 운영체제에서 비필수 동작 최소화를 요청했음을 의미하므로 animation duration만 줄이는 데 그치지 말고 반복·이동 효과 의존을 제거한다.

### Project Context Reference

- Story 원문: [Source: _bmad-output/planning-artifacts/epics.md#story-12-안전하고-접근-가능한-데스크톱-앱-셸]
- Electron 보안 결정: [Source: _bmad-output/planning-artifacts/architecture.md#인증-및-보안]
- 프런트엔드 구조: [Source: _bmad-output/planning-artifacts/architecture.md#프런트엔드-아키텍처]
- 프로젝트 디렉터리: [Source: _bmad-output/planning-artifacts/architecture.md#프로젝트-구조-및-경계]
- 디자인 토큰과 컴포넌트: [Source: _bmad-output/planning-artifacts/ux-designs/ux-gracetree-shorts-studio-2026-06-19/DESIGN.md]
- 화면·상태·접근성 계약: [Source: _bmad-output/planning-artifacts/ux-designs/ux-gracetree-shorts-studio-2026-06-19/EXPERIENCE.md]
- 기준 시안: [Source: _bmad-output/planning-artifacts/ux-designs/ux-gracetree-shorts-studio-2026-06-19/mockups/studio-black-reference.html#direction-a]
- Electron Security: https://www.electronjs.org/docs/latest/tutorial/security
- Electron Context Isolation: https://www.electronjs.org/docs/latest/tutorial/context-isolation
- Electron Process Sandboxing: https://www.electronjs.org/docs/latest/tutorial/sandbox
- WCAG Contrast Minimum: https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html
- WCAG Reflow: https://www.w3.org/WAI/WCAG22/Understanding/reflow.html
- Reduced Motion: https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/prefers-reduced-motion

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Implementation Plan

- Story 1.1 baseline과 기존 Electron 수명주기를 보존하면서 창 보안 옵션과 navigation 정책을 테스트 가능한 모듈로 분리한다.
- contracts가 소유하는 빈 `desktopApi` 타입 경계를 preload에 노출하고 renderer에는 Node/Electron 권한을 전달하지 않는다.
- Studio Black 토큰, semantic shell, view state와 접근 가능한 설정 dialog를 구현한다.
- Vitest 컴포넌트·보안 테스트와 Playwright Electron E2E로 창 크기, 확대, 오프라인 및 외부 navigation 차단을 검증한다.

### Debug Log References

- Create-story customization resolver는 시스템 Python 3.9.6에 `tomllib`이 없어 실행되지 않았다. 스킬 지정 fallback 규칙으로 TOML을 직접 병합했다.
- Story 1.1 커밋 `38ceeee`와 실제 UPDATE 파일을 다시 읽고 stale baseline, preload 계약, CSP 및 테스트 경계를 갱신했다.
- RED 단계에서 누락된 shell 컴포넌트·스타일·보안 모듈로 인해 desktop 테스트가 실패하는 것을 확인한 뒤 구현했다.
- sandbox preload가 workspace 계약을 runtime `require`해 `desktopApi`를 노출하지 못한 문제를 type-only 계약 import와 로컬 typed key로 수정했다.
- Playwright Electron 테스트의 CommonJS 환경에서 `import.meta` 사용이 실패해 `__filename`/`__dirname` 기반 경로로 수정했다.
- `pnpm --filter gracetree-desktop test`, `typecheck`, `lint`, production build와 Electron Playwright E2E를 통과했다.
- 기본 `python3`에는 pytest가 없어 첫 전체 CI가 Python 단계에서 중단됐고, 저장소 `.venv`를 지정한 `PYTHON=.venv/bin/python pnpm run ci`로 contracts Vitest 6개, desktop Vitest 9개, pytest 9개, Node↔Python 통합, 타입 검사와 lint를 통과했다.

### Completion Notes List

- Ultimate context engine analysis completed - comprehensive developer guide created.
- 1180×720 최소 창, Electron sandbox/context isolation, navigation/new-window 차단과 local-only CSP를 적용했다.
- 동결된 빈 `desktopApi` 계약을 공유 패키지에 추가하고 raw IPC·Node API가 renderer에 노출되지 않도록 검증했다.
- Studio Black 토큰, 홈·가이드 shell, 78px sidebar, 70px status bar와 접근 가능한 설정 dialog를 구현했다.
- 키보드 활성화, focus trap/복원, reduced motion, 대비, 200% 확대, 오프라인과 외부 navigation 차단을 자동 테스트로 검증했다.
- Story 1.2 구현과 로컬 검증을 완료해 `review` 상태로 전환했다. 원격 GitHub Actions 실행은 확인하지 않았다.

### File List

- `.github/workflows/ci.yml`
- `_bmad-output/implementation-artifacts/1-2-안전하고-접근-가능한-데스크톱-앱-셸.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `apps/desktop/.gitignore`
- `apps/desktop/package.json`
- `apps/desktop/playwright.config.ts`
- `apps/desktop/src/main/index.ts`
- `apps/desktop/src/main/navigation-policy.test.ts`
- `apps/desktop/src/main/navigation-policy.ts`
- `apps/desktop/src/main/window-options.test.ts`
- `apps/desktop/src/main/window-options.ts`
- `apps/desktop/src/preload/desktop-api.test.ts`
- `apps/desktop/src/preload/desktop-api.ts`
- `apps/desktop/src/preload/index.d.ts`
- `apps/desktop/src/preload/index.ts`
- `apps/desktop/src/renderer/index.html`
- `apps/desktop/src/renderer/src/App.test.tsx`
- `apps/desktop/src/renderer/src/App.tsx`
- `apps/desktop/src/renderer/src/assets/base.css` (deleted)
- `apps/desktop/src/renderer/src/assets/electron.svg` (deleted)
- `apps/desktop/src/renderer/src/assets/main.css` (deleted)
- `apps/desktop/src/renderer/src/assets/wavy-lines.svg` (deleted)
- `apps/desktop/src/renderer/src/components/SettingsDialog.tsx`
- `apps/desktop/src/renderer/src/components/SidebarIcon.tsx`
- `apps/desktop/src/renderer/src/main.tsx`
- `apps/desktop/src/renderer/src/security.test.ts`
- `apps/desktop/src/renderer/src/styles/App.module.css`
- `apps/desktop/src/renderer/src/styles/globals.css`
- `apps/desktop/src/renderer/src/styles/tokens.css`
- `apps/desktop/src/renderer/src/styles/tokens.test.ts`
- `apps/desktop/src/renderer/src/test/setup.ts`
- `apps/desktop/tests/e2e/desktop-shell.spec.ts`
- `apps/desktop/vitest.config.ts`
- `package.json`
- `packages/contracts/package.json`
- `packages/contracts/src/desktop-api.ts`
- `pnpm-lock.yaml`

### Change Log

- 2026-06-20: Story 1.2 데스크톱 앱 셸, 최소 권한 preload 경계, 디자인 토큰, 접근성·보안 테스트와 Electron E2E 구현 완료.
